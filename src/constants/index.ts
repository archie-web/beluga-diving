import { ZINDEX } from './zIndex';

export const transitionDuration = 'ease-in-lazy transition-all duration-500';

export const enum Sizes {
   SM = 'sm',
   BASE = 'base',
   MD = 'md',
   LG = 'lg',
   XL = 'xl',
}

export { ZINDEX };
